streamlit>=1.36
pandas>=2.0
plotly>=5.20
pillow>=10.0
numpy>=1.24

# Optional real OCR engine.
# Install only when the machine has a working Python environment and enough time
# for model downloads:
# paddleocr>=2.7
# paddlepaddle>=2.6
